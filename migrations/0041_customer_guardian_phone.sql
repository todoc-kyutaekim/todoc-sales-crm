-- 고객 편집 폼 필드 순서 재배치(사용자 요청)에 따라 '보호자 연락처' 입력칸을 추가합니다.
-- 요청 순서: 1 이름 → 2 성별 → 3 생년월일 → 4 병원 → 5 연락처1 → 6 보호자 연락처1 → 7 이메일 → 8 주소
--
-- ⚠️ 기존 `guardian_of`(보호자 고객 ID, FK) 와는 전혀 다른 필드입니다.
--    guardian_of      : 다른 고객 레코드를 보호자로 연결하는 참조 (기존 유지)
--    guardian_phone   : 보호자에게 직접 연락할 전화번호 (신규, 자유 텍스트)
--    보호자를 고객으로 따로 등록하지 않아도 연락처만 남길 수 있게 하기 위함입니다.
--
-- ⚠️ DEFAULT '' 로 두어 기존 10건은 빈 문자열이 됩니다.
--    NULL 이 아니라 '' 인 이유: 다른 연락 필드(phone/email/address)가 모두
--    `b.phone || ''` 패턴으로 '' 를 저장하므로 표시 로직을 일관되게 유지합니다.
ALTER TABLE customers ADD COLUMN guardian_phone TEXT DEFAULT '';
